<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.co.blogspot.abdullahxcreator.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ikon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="android.support.multidex.MultiDexApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashScreen" />
        <activity android:name=".MenuUtama" />
        <activity android:name=".MenuChef" />
        <activity android:name=".MenuCake" />
        <activity android:name=".MenuTutorial" />
        <activity android:name=".DetailCake" />
        <activity android:name=".DetailTutorial" />
        <activity android:name=".DetailChef" />
    </application>

</manifest>